# 複製成 .env 或直接設成環境變數。全部選用 — 未設定時對應功能會自動略過。

# AI 族群/題材分類 + 今日國際重點 (預設走免費 GitHub Models)。
# 一支具 Models:read 權限的 GitHub fine-grained token。
AI_API_KEY=

# 可選: 切換到其他 OpenAI 相容服務 (Groq / OpenRouter / OpenAI ...)。
# AI_BASE_URL=https://models.github.ai/inference
# AI_MODEL=openai/gpt-4.1

# FinMind 備援 (yfinance 抓不到時改用)。免費註冊 finmindtrade.com 取得。
# 未設定則只用 yfinance。
FINMIND_TOKEN=
